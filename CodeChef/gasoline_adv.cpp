#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int test;
    cin>>test;
    while(test--){
        int N;
        cin>>N;
        int fuel[N];
        for(int i=0;i<N;i++)cin>>fuel[i];
        int cost[N];
        for(int i=0;i<N;i++)cin>>cost[i];

        
           
    }
    return 0;
}