#include <iostream>
#include <queue>

using namespace std;

struct Node{
    int data;
    Node* left, right;
};

Node* root = NULL;

void createNode(int ){
    Node* newNode = new Node();
    
}

int deepestNode(){
    Node* temp;

}