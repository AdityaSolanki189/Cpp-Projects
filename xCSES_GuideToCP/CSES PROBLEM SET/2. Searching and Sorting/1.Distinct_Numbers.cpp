#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
	cin.tie(0);
	cout.tie(0);
	ios_base::sync_with_stdio(0);

	ll num,sum,wsum;
	cin>>num;
    vector<int> x;

    if(n*(n+1) %4 != 0){
    	cout<<"NO";
    	return 0;
    }



	return 0;
}