#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
	cin.tie(0);
	cout.tie(0);
	ios_base::sync_with_stdio(0);

	ll num;
	vector<ll> x;
	cin>>num;
	while(num--){
		for ( ll i = 0; i < num; i++ )
			cin>>x[i];
		
	}
	return 0;
}